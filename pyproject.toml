[project]
name = "youtube-transcriber"
version = "0.4.0"
description = "Download YouTube audio and transcribe locally using faster-whisper. Designed for LLM ingestion via CLI."
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "Steve Overton" }]
requires-python = ">=3.11"
keywords = ["youtube", "transcription", "whisper", "speech-to-text", "ai", "llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: MacOS :: MacOS X",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Multimedia :: Sound/Audio :: Speech",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    # YouTube changes its extraction surface constantly and yt-dlp ships fixes within days,
    # so a stale yt-dlp is the top cause of download failure. Keep this floor current.
    "yt-dlp>=2026.8.19",
    "faster-whisper>=1.0.0",
    "click>=8.1.0",
    # Backs yt-dlp's Linux keyring cookie path, which is what --cookies-from-browser
    # drives. macOS uses the Keychain and Windows its own DPAPI decryptor, so neither
    # ever reaches this dependency.
    "secretstorage>=3.5.0; sys_platform == 'linux'",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-mock>=3.12.0",
    "ruff>=0.4.0",
]
# Apple Silicon: GPU-accelerated transcription via Apple MLX framework.
# Install with: uv sync --extra mlx
# mlx-whisper uses Metal GPU and Apple Neural Engine instead of CPU.
mlx = [
    "mlx-whisper>=0.4.0",
]

[project.scripts]
youtube-transcriber = "youtube_transcriber.cli:cli"

[project.urls]
Homepage = "https://github.com/overtonlabs/youtube-transcriber"
Repository = "https://github.com/overtonlabs/youtube-transcriber"
Issues = "https://github.com/overtonlabs/youtube-transcriber/issues"
Changelog = "https://github.com/overtonlabs/youtube-transcriber/blob/main/CHANGELOG.md"
Documentation = "https://github.com/overtonlabs/youtube-transcriber#readme"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/youtube_transcriber"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]
target-version = "py311"

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # flake8-bugbear
    "C4",  # flake8-comprehensions
    "UP",  # pyupgrade
]
ignore = [
    "E501", # line too long (handled by formatter)
]

[tool.ruff.lint.isort]
known-first-party = ["youtube_transcriber"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
